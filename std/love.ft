# love.ft
# 2021-08-13 14:23:37

use std
use os

def lit []string = [
    "&", "@", "#", "*", "$", "+", "%", "0"
]

def (p string) heart
    def x float = 0.
    def y float = 0.

    def s string = ""
    def i float = 1.5

    aop i > -1.5
        s = ""
        x = -1.5
        aop x < 1.5
            y = x * x + i * i - 1
            if (y * y * y -
                x * x * i * i * i) < 0
                def r int = std.rand(0, std.len(lit))
                if r >= 3
                    s += lit[r]
                nf
                    s += p
            nf
                s += " "
            x += 0.05
        std.println(s)
        i -= 0.1

def () show
    for def i int = 0; i < 8; i += 1
        heart(lit[i])
        os.sleep(1000)
    std.println("
    ❤   ❤
   ❤  +  ❤
    ❤   ❤
      ❤
                👨‍💻🤪 👏 happy to you!
    ")
show()